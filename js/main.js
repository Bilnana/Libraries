jQuery(($) => {
  const jsLoaded = () => {
    console.log('js is loaded');
  }

  /* ====== DOCUMENT READY ====== */
  $(document).ready(() => {
    jsLoaded ();
  });

   /* ====== resize ====== */
  // $(window).resize(() => {
 
  // });

  /* ====== scroll ====== */
  // $(window).scroll((e) => {
  
  // });
});